import 'package:flutter/material.dart';

class ViewSecondOpinion extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Container(
        child: Text('View second opinion'),
      ),
    );
  }
}
